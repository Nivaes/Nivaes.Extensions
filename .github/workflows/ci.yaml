name: CI

on:
  push:
    branches-ignore:
    - 'release/v**'  
    paths-ignore:
    - '**/*.md'
    - '**/*.sln'
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
    - '**/*.md'
    - '**/*.sln'

jobs:
  build:

    uses: Nivaes/Nivaes.Workflows/.github/workflows/basic_lib_ci.yaml@develop
