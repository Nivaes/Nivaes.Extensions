name: Publish Release

on:
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  build_release:
    uses: Nivaes/Nivaes.Workflows/.github/workflows/basic_lib_publish_release.yaml@develop
    secrets:
      nuge_token: ${{ secrets.NIVAES_NUGET_TOKEN }}
